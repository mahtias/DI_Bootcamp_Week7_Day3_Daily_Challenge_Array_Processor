/**
 * @author N'ZO LAGOU
 * 
 *1) Écrivez quelques expressions lambda et une 
fonction qui renvoie une expression lambda comme valeur.

Supposons qu'une interface de fonction 
ArrayProcessorest définie comme
 */


package array.processor;

public interface ArrayProcessor {
	double apply(double[] array);
}
